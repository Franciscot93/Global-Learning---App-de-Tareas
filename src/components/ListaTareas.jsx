import React from 'react'

export default function ListaTareas({tareas}) {
  return (
    <div>
        {
            tareas.map((tareas)=>{
                return 
            })
        }
    </div>
  )
}
